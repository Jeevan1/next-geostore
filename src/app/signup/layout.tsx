export const metadata = {
  title: {
    default: "Sign Up",
    template: "%s - GEOSTORE",
  },
};

export default function SignupLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
